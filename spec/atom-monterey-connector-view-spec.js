'use babel';

import AtomMontereyConnectorView from '../lib/atom-monterey-connector-view';

describe('AtomMontereyConnectorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
